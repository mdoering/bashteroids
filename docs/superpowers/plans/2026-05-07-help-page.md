# Help Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an in-game help screen explaining controls, powerups, and enemies, opened from the title via the `H` key or a tappable `?` button in the lower-right corner.

**Architecture:** A new full-screen `HelpScene` replaces the title when opened, returns to a fresh `TitleScene` on dismiss. `TitleScene` gains a tappable help button and an `H`-key shortcut. No new types, no new managers — reuses `KeyboardManager.shared` and `ControllerManager.shared`.

**Tech Stack:** SpriteKit + Swift 5, single Xcode target (iOS / Mac Catalyst / tvOS).

**Spec:** `docs/superpowers/specs/2026-05-07-help-page-design.md`

---

## File Structure

- **Create:** `Bashteroids/Scenes/HelpScene.swift` — new scene with two-column help layout, dismissal handlers
- **Modify:** `Bashteroids/Scenes/TitleScene.swift` — `?` button node, `touchesBegan` hit-test, `H`-key case in `handleKeyDown`

The `Bashteroids/` folder is `PBXFileSystemSynchronizedRootGroup`, so dropping `HelpScene.swift` under `Scenes/` auto-includes it. No `project.pbxproj` edits.

---

### Task 1: Create HelpScene with content layout

**Files:**
- Create: `Bashteroids/Scenes/HelpScene.swift`

- [ ] **Step 1: Create the HelpScene file with title, columns, and dismiss hint**

Drop this complete file at `Bashteroids/Scenes/HelpScene.swift`:

```swift
import SpriteKit
import GameController

final class HelpScene: SKScene {
    private let manager = ControllerManager.shared
    private var transitioning = false
    private var prevPressed: [ObjectIdentifier: Bool] = [:]

    private let goldColor   = SKColor(red: 245/255, green: 194/255, blue: 66/255, alpha: 1)
    private let labelColor  = SKColor.white
    private let valueColor  = SKColor(white: 0.65, alpha: 1)
    private let dimColor    = SKColor(white: 0.45, alpha: 1)

    override func didMove(to view: SKView) {
        backgroundColor = .black

        let title = SKLabelNode(text: "HELP")
        title.fontName = "AvenirNext-Bold"
        title.fontSize = 28
        title.fontColor = goldColor
        title.position = CGPoint(x: size.width / 2, y: size.height * 0.93)
        addChild(title)

        let leftX  = size.width * 0.10
        let rightX = size.width * 0.55
        let topY   = size.height * 0.85

        renderControls(originX: leftX, topY: topY)
        renderPowerups(originX: rightX, topY: topY)
        renderEnemies(originX: rightX, topY: topY - 240)

        let hint = SKLabelNode(text: "PRESS ANY BUTTON / SPACE / ESC TO RETURN")
        hint.fontName = "AvenirNext-Regular"
        hint.fontSize = 14
        hint.fontColor = dimColor
        hint.position = CGPoint(x: size.width / 2, y: size.height * 0.05)
        addChild(hint)

        KeyboardManager.shared.onKeyDown = { [weak self] code in
            self?.handleKeyDown(code)
        }
    }

    override func willMove(from view: SKView) {
        KeyboardManager.shared.onKeyDown = nil
    }

    private func renderControls(originX: CGFloat, topY: CGFloat) {
        var y = topY
        addHeading("CONTROLS", x: originX, y: y); y -= 28

        addHeading("KEYBOARD", x: originX, y: y, small: true); y -= 22
        for (label, value) in [
            ("Turn",      "← / →"),
            ("Thrust",    "↑"),
            ("Brake",     "↓"),
            ("Fire",      "Space"),
            ("Minelayer", "M"),
            ("Join",      "A"),
            ("Start",     "Space / Enter")
        ] {
            addRow(label: label, value: value, x: originX, y: y); y -= 18
        }

        y -= 10
        addHeading("CONTROLLER", x: originX, y: y, small: true); y -= 22
        for (label, value) in [
            ("Turn",      "Left stick / D-pad"),
            ("Thrust",    "A / R-trigger"),
            ("Brake",     "B / R-stick down"),
            ("Fire",      "X / R-shoulder"),
            ("Minelayer", "Y"),
            ("Join",      "A"),
            ("Start",     "Menu / X / Play-Pause")
        ] {
            addRow(label: label, value: value, x: originX, y: y); y -= 18
        }
    }

    private func renderPowerups(originX: CGFloat, topY: CGFloat) {
        var y = topY
        addHeading("POWERUPS", x: originX, y: y); y -= 28

        for (label, value) in [
            ("Shield",     "Absorbs 1 hit. Stacks 2x."),
            ("Dual-canon", "Faster fire, stacks to quad."),
            ("Boost",      "+43% / +79% max speed."),
            ("Minelayer",  "Place mine, re-press to blow.")
        ] {
            addRow(label: label, value: value, x: originX, y: y); y -= 22
        }
    }

    private func renderEnemies(originX: CGFloat, topY: CGFloat) {
        var y = topY
        addHeading("ENEMIES", x: originX, y: y); y -= 28

        for (label, value) in [
            ("Asteroid",     "1 hit, drifts, wraps."),
            ("UFO",          "1 hit, fires aimed bullets."),
            ("Alien",        "2 hits, short laser."),
            ("Snake",        "4 hits, homes on you."),
            ("Mine",         "Flashes 6 s, 140 px blast."),
            ("Rock",         "Indestructible."),
            ("Wall (gray)",  "BATTLE: indestructible."),
            ("Wall (orange)","BATTLE: 5 hp / chunk.")
        ] {
            addRow(label: label, value: value, x: originX, y: y); y -= 18
        }
    }

    private func addHeading(_ text: String, x: CGFloat, y: CGFloat, small: Bool = false) {
        let label = SKLabelNode(text: text)
        label.fontName = "AvenirNext-Bold"
        label.fontSize = small ? 14 : 18
        label.fontColor = goldColor
        label.horizontalAlignmentMode = .left
        label.verticalAlignmentMode = .top
        label.position = CGPoint(x: x, y: y)
        addChild(label)
    }

    private func addRow(label: String, value: String, x: CGFloat, y: CGFloat) {
        let lbl = SKLabelNode(text: label)
        lbl.fontName = "AvenirNext-Regular"
        lbl.fontSize = 14
        lbl.fontColor = labelColor
        lbl.horizontalAlignmentMode = .left
        lbl.verticalAlignmentMode = .top
        lbl.position = CGPoint(x: x, y: y)
        addChild(lbl)

        let val = SKLabelNode(text: value)
        val.fontName = "AvenirNext-Regular"
        val.fontSize = 14
        val.fontColor = valueColor
        val.horizontalAlignmentMode = .left
        val.verticalAlignmentMode = .top
        val.position = CGPoint(x: x + 130, y: y)
        addChild(val)
    }

    // MARK: - Dismissal

    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        returnToTitle()
    }

    private func handleKeyDown(_ code: GCKeyCode) {
        if code == .escape || code == .spacebar { returnToTitle() }
    }

    override func update(_ currentTime: TimeInterval) {
        guard !transitioning else { return }
        for c in manager.connectedControllers {
            let id = ObjectIdentifier(c)
            let pressed = anyButtonPressed(c)
            let was = prevPressed[id] ?? false
            if pressed && !was { returnToTitle(); break }
            prevPressed[id] = pressed
        }
    }

    private func anyButtonPressed(_ c: GCController) -> Bool {
        if let gp = c.extendedGamepad {
            return gp.buttonA.isPressed || gp.buttonB.isPressed
                || gp.buttonX.isPressed || gp.buttonY.isPressed
                || gp.buttonMenu.isPressed
        }
        if let mg = c.microGamepad {
            return mg.buttonA.isPressed || mg.buttonX.isPressed
        }
        return false
    }

    private func returnToTitle() {
        guard !transitioning else { return }
        transitioning = true
        let next = TitleScene(size: size)
        next.scaleMode = scaleMode
        view?.presentScene(next, transition: .fade(withDuration: 0.3))
    }
}
```

- [ ] **Step 2: Build for iOS to confirm the new file compiles**

Run:
```sh
xcodebuild -project Bashteroids.xcodeproj -scheme Bashteroids \
           -destination 'generic/platform=iOS' \
           -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5
```
Expected: `** BUILD SUCCEEDED **` with zero warnings.

(No commit yet — TitleScene wiring lands in the same commit at the end of Task 2.)

---

### Task 2: Wire help button + H key into TitleScene

**Files:**
- Modify: `Bashteroids/Scenes/TitleScene.swift`

- [ ] **Step 1: Add helpButton property near the other UI properties**

In `TitleScene.swift`, find the property list near the top of the class (around line 17, alongside `modeLabel`, `levelLabel`, `battleHintLabel`):

```swift
    private var modeLabel: SKLabelNode!
    private var levelLabel: SKLabelNode!
    private var battleHintLabel: SKLabelNode!
```

Add directly after:

```swift
    private var helpButton: SKShapeNode!
```

- [ ] **Step 2: Construct the help button in didMove(to:)**

In `didMove(to:)`, find the section that adds the `BATTLE NEEDS 2+ PLAYERS` hint label and ends with `self.battleHintLabel = battleHint`. Right after that block (before `renderSelectors()`), insert:

```swift
        let helpBtn = SKShapeNode(circleOfRadius: 22)
        helpBtn.position = CGPoint(x: size.width - 50, y: 50)
        helpBtn.strokeColor = SKColor(white: 0.55, alpha: 1)
        helpBtn.fillColor = .clear
        helpBtn.lineWidth = 1.5
        addChild(helpBtn)

        let helpQ = SKLabelNode(text: "?")
        helpQ.fontName = "AvenirNext-Bold"
        helpQ.fontSize = 22
        helpQ.fontColor = SKColor(white: 0.55, alpha: 1)
        helpQ.verticalAlignmentMode = .center
        helpQ.horizontalAlignmentMode = .center
        helpBtn.addChild(helpQ)
        self.helpButton = helpBtn
```

- [ ] **Step 3: Add touchesBegan for tap-on-button hit testing**

`SKScene` already inherits from `UIResponder`, so the `touchesBegan` override compiles on iOS, Mac Catalyst, and tvOS without additional imports. Add this method to the class (anywhere — same level as `tryStart`, `update`, etc.):

```swift
    override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
        guard let t = touches.first else { return }
        let p = t.location(in: self)
        if helpButton != nil, helpButton.frame.contains(p) {
            openHelp()
        }
    }
```

The `helpButton != nil` guard is just defensive: if the scene is dismissed mid-touch, the implicitly-unwrapped optional would crash without it.

- [ ] **Step 4: Add openHelp() method**

Add to the class:

```swift
    private func openHelp() {
        guard !transitioning, activeNameSlot == nil else { return }
        transitioning = true
        let help = HelpScene(size: size)
        help.scaleMode = scaleMode
        view?.presentScene(help, transition: .fade(withDuration: 0.3))
    }
```

The `activeNameSlot == nil` guard prevents opening help while a name is being typed (the `H` keypress would otherwise be both a name character and a help shortcut).

- [ ] **Step 5: Wire `H` key into handleKeyDown**

In `handleKeyDown(_:)`, find the second `switch` that handles `.spacebar`, `.escape`, etc. (around line 337). Add a `.keyH` case before `.spacebar`:

```swift
        switch code {
        case .keyA:
            ...existing keyA handler...
        case .keyH:
            openHelp()
        case .spacebar, .returnOrEnter, .keypadEnter:
            tryStart()
        case .escape:
            MacFullScreen.exitIfActive()
        default:
            break
        }
```

`H` only fires here when `activeNameSlot == nil` is true (the early `return` block at the top of `handleKeyDown` handles the name-entry case, where `H` is a typeable character). So pressing `H` while typing a name still types `H` — only `H` outside name entry opens help.

- [ ] **Step 6: Build all three platforms**

Run each in turn:

```sh
xcodebuild -project Bashteroids.xcodeproj -scheme Bashteroids \
           -destination 'generic/platform=iOS' \
           -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5

xcodebuild -project Bashteroids.xcodeproj -scheme Bashteroids \
           -destination 'platform=macOS,variant=Mac Catalyst' \
           -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5

xcodebuild -project Bashteroids.xcodeproj -scheme Bashteroids \
           -destination 'generic/platform=tvOS' \
           -configuration Debug build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -5
```
Each must end with `** BUILD SUCCEEDED **` and zero warnings.

- [ ] **Step 7: Commit**

```bash
git add Bashteroids/Scenes/HelpScene.swift Bashteroids/Scenes/TitleScene.swift docs/superpowers/specs/2026-05-07-help-page-design.md docs/superpowers/plans/2026-05-07-help-page.md
git commit -m "$(cat <<'EOF'
feat: in-game help page from title screen

Adds HelpScene listing controls (keyboard + controller), powerups,
and enemies. Opened via the H key or by tapping a ? button in the
lower-right corner of the title. Dismissed with ESC, SPACE, any
controller button, or a tap.

Controller-only invocation deferred to feature F (universal
navigation). H-key shortcut is gated on name-entry so typing 'H'
into a player name still types the letter.
EOF
)"
```

---

## Notes for the implementer

- The `SKShapeNode.frame` returned in `touchesBegan` reflects the node's bounding rect in scene coordinates — direct hit-test, no extra transform math needed.
- `HelpScene` deliberately does **not** wire `KeyboardManager.shared.onKeyDown` in a way that conflicts with `TitleScene`'s handler. The pattern in this codebase is "current scene owns the handler" — `didMove(to:)` installs, `willMove(from:)` clears. `HelpScene` replaces the closure on entry, restores nothing on exit (the new `TitleScene` constructed by `returnToTitle()` will install its own handler in its `didMove(to:)`).
- `prevPressed` in `HelpScene.update` is keyed on `ObjectIdentifier(GCController)` — the same pattern as `menuWasPressed`/`xWasPressed` in `TitleScene`. Edge-trigger so a controller button held during the transition into help doesn't immediately dismiss.
- No tests in this codebase. Verification is `xcodebuild` plus the manual checks called out in CLAUDE.md (Siri Remote / second MFi controller / focus parallax).
